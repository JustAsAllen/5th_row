---
description: EXPERIMENT LAB. Try an uncertain idea in an isolated scratch sandbox, benchmark it, then integrate or discard without contaminating the main tree.
agent: build
---

Experiment lab: $ARGUMENTS

Run this in an ISOLATED sandbox (e.g. `scratch/<slug>/` outside the app's src):
1. Restate the experiment + hypothesis + success criteria.
2. Build the smallest isolated prototype that tests the idea.
3. Benchmark/verify against the current approach (real measurements).
4. Decide: INTEGRATE (move into the app, wire in, verify) or DISCARD (remove
   sandbox, record the rejection in project memory).
5. Report the decision + evidence. The main project tree is never contaminated.