---
description: GOATED MODE. "Make it goated" = improve architecture, UI, UX, responsiveness, animations, a11y, performance, error/loading/empty states, micro-interactions, code quality — then verify everything. No random feature bloat.
agent: build
---

Make it goated: $ARGUMENTS

Interpret "goated/premium/polish" as a FULL upgrade, not a tweak:
- architecture & code quality (no random complexity)
- UI/UX: design DNA present → enhance; else establish then polish (design-intelligence + ui-ux-premium)
- responsiveness: visual QA across breakpoints
- animations/micro-interactions: rive/framer-motion tastefully (never gratuitous)
- accessibility, performance, error/loading/empty states
- reduce unnecessary complexity; kill features that add bulk without value

Do NOT add random features to make the project bigger.

Verify each improvement actually landed: typecheck + lint + build, and for web
apps screenshots before/after across breakpoints. Report IMPLEMENTED/TESTED/
VERIFIED/KNOWN LIMITATIONS. Update project memory with the accepted design DNA.